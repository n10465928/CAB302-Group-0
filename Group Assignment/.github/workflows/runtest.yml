name: Run All Tests

on:
  pull_request:
    branches:
      - 'master'
  push:
  workflow_dispatch:

jobs:
  tests:
    runs-on: windows-latest

    steps:
      - name: Install Dependencies
        run: npm ci
      - name: Run tests
        run: npm test